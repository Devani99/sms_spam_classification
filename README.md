# sms_spam_classification
Spam messages are unwanted messages that often contain advertisements, scams, or phishing links. This project uses Machine Learning and NLP techniques to automatically detect whether an SMS message is spam or not.  The model is trained on an SMS dataset and deployed using Streamlit so users can test messages in real time.
